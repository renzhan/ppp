# 复盘报告章节数据字段映射与生成规范

> 基于业务字段映射表、PDF模版分析、系统已实现的数据库/API/计算引擎，整理每个报告章节所需全部数据。

---

## 一、数据源与系统字段对照总表

### 1.1 蒲公英数据 → DB: notes 表

| 业务字段 | Prisma字段 | DB列名 | 类型 |
|---------|-----------|--------|------|
| 笔记ID | noteId | note_id | VarChar(100) |
| 博主昵称 | kolNickName | kol_nick_name | VarChar(200) |
| 博主粉丝量 | kolFanNum | kol_fan_num | Int |
| 笔记类型 | noteType | note_type | VarChar(20) |
| 笔记链接 | noteLink | note_link | Text |
| 曝光量 | impNum | imp_num | Int |
| 阅读量 | readNum | read_num | Int |
| 点赞量 | likeNum | like_num | Int |
| 收藏量 | favNum | fav_num | Int |
| 评论量 | cmtNum | cmt_num | Int |
| 分享量 | shareNum | share_num | Int |
| 关注量 | followNum | follow_num | Int |
| 博主报价 | kolPrice | kol_price | Decimal(12,2) |
| 服务费 | serviceFee | service_fee | Decimal(12,2) |
| 平台总价 | totalPlatformPrice | total_platform_price | Decimal(12,2) |
| 加热曝光 | heatImpNum | heat_imp_num | Int |
| 加热阅读 | heatReadNum | heat_read_num | Int |
| 是否水下 | isUnderwater | is_underwater | Boolean |
| 水下报价 | underwaterPrice | underwater_price | Decimal(12,2) |
| 组件数据 | components | components | Json |

### 1.2 聚光数据 → DB: juguang_data 表

| 业务字段 | Prisma字段 | DB列名 | 类型 |
|---------|-----------|--------|------|
| 笔记ID | noteId | note_id | VarChar(100) |
| 消费 | fee | fee | Decimal(12,2) |
| 展现量 | impression | impression | Int |
| 点击量 | click | click | Int |
| 互动量 | interaction | interaction | Int |
| 种草人群 | iUserNum | i_user_num | Int |
| 深度种草人群 | tiUserNum | ti_user_num | Int |
| 种草成本 | iUserPrice | i_user_price | Decimal(12,4) |
| 深度种草成本 | tiUserPrice | ti_user_price | Decimal(12,4) |
| 搜索组件点击 | searchCmtClick | search_cmt_click | Int |
| 搜索后阅读 | searchCmtAfterRead | search_cmt_after_read | Int |
| 搜索后均阅读 | searchCmtAfterReadAvg | search_cmt_after_read_avg | Decimal |
| 搜索点击转化率 | searchCmtClickCvr | search_cmt_click_cvr | Decimal |

### 1.3 业务标注 → DB: business_annotations 表

| 业务字段 | Prisma字段 | DB列名 | 类型 |
|---------|-----------|--------|------|
| 笔记ID | noteId | note_id | VarChar(100) |
| 内容方向 | contentDirection | content_direction | VarChar(100) |
| 账号类型 | accountType | account_type | VarChar(100) |
| KOL类型 | kolType | kol_type | VarChar(100) |
| 投放阶段 | launchPhase | launch_phase | VarChar(100) |
| 是否水下 | isUnderwater | is_underwater | Boolean |

### 1.4 灵犀数据 → DB: lingxi_data 表

| 业务字段 | Prisma字段 | DB列名 | 说明 |
|---------|-----------|--------|------|
| 数据类型 | dataType | data_type | aips/brand_ranking/soc_sov/spu_ranking |
| 数据内容 | dataContent | data_content | Json结构化 |

### 1.5 人工录入 → DB: manual_inputs + kpi_targets 表

| 业务字段 | 表 | 字段 | 说明 |
|---------|---|------|------|
| 大盘基准 | manual_inputs | dataContent (inputType=benchmark) | {ctr,cpm,cpc,cpe} |
| KPI目标 | kpi_targets | metricName + targetValue | 各指标目标值 |

### 1.6 复盘配置 → DB: review_configs 表

| 业务字段 | Prisma字段 | 说明 |
|---------|-----------|------|
| 大盘基准 | benchmark | Json |
| 达人层级 | influencerTiers | Json数组 |
| KPI目标 | kpiTargets | Json |
| 互动口径 | engagementMetric | exclude_follow/include_follow |
| 爆文方式 | viralMetric | like_comment_share/like_only |
| 模块开关 | modules | Json |
| 投放阶段 | launchPhases | Json数组 |

### 1.7 项目信息 → DB: projects 表

| 业务字段 | Prisma字段 | 说明 |
|---------|-----------|------|
| 品类 | category | |
| 品牌 | brand | |
| 业务线 | businessLine | |
| 项目名称 | projectName | |
| 开始日期 | startDate | |
| 结束日期 | endDate | |
| 项目类型 | projectType | |
| 互动配置 | engagementConfig | {includeShare,includeFollow} |


---

## 二、各章节数据需求与计算规范

### 第1章：封面（1页）

**所需数据：**

| 输出值 | 计算/来源 | 系统字段 | 状态 |
|-------|----------|---------|------|
| 品类 | 直接引用 | `projects.category` | ✅已实现 |
| 合作品牌 | 直接引用 | `projects.brand` | ✅已实现 |
| 品牌业务线 | 直接引用 | `projects.business_line` | ✅已实现 |
| 项目名称 | 直接引用 | `projects.project_name` | ✅已实现 |
| 项目周期 | 直接引用 | `projects.start_date` ~ `projects.end_date` | ✅已实现 |

**生成提示词规范：**
```
封面展示：品牌名 + 项目名称 + "小红书种草/传播项目复盘" + 项目周期(yyyy/mm/dd—yyyy/mm/dd)
```

---

### 第2章：项目回顾（2-3页）

**所需数据：**

| 输出值 | 计算/来源 | 系统字段 | 状态 |
|-------|----------|---------|------|
| 项目背景 | AI解析策划案 | `ai_generated_content.content_type='plan_parse'` → generatedContent.projectObjective | ✅已实现 |
| 传播目的 | AI解析策划案 | `ai_generated_content.content_type='plan_parse'` → generatedContent.strategy | ✅已实现 |
| 策略回顾 | AI解析策划案 | `ai_generated_content.content_type='plan_parse'` → generatedContent.targetAudience | ✅已实现 |
| 传播节奏 | 复盘配置 | `review_configs.launch_phases` | ✅已实现 |
| 数据解读 | AI生成 | 叙事引擎M2模块 | ✅已实现 |

**数据获取方式：**
- 策划案通过 `/api/upload/plan-parse` 上传PDF，系统使用Vision LLM解析
- 解析结果存入 `ai_generated_content` 表，contentType='plan_parse'
- 叙事引擎使用 `prompts/{projectType}/M2_review/{tone}.yaml` 模板生成

**生成提示词规范：**
```
基于策划案解析结果，输出以下内容：
1. 项目背景（客户期待）：定位策划案中的传播目标和品牌诉求，50-100字概述
2. 传播目的：提炼核心传播目标，分维度列出（品牌价值/产品声量/活动造势）
3. 策略回顾：按时间轴梳理各阶段策略要点和发布节奏
若策划案中未找到相关内容，标注「未找到」
```

---

### 第3章：数据总览（2-4页）

**所需数据与计算公式：**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 总笔记篇数 | COUNT(notes) | `notes.note_id` WHERE project_id | `pipeline.ts` → noteCount | ✅已实现 |
| **总曝光** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='impression' | KpiTarget model | ✅已实现 |
| - 实际达成 | SUM(imp_num) | `notes.imp_num` | `pipeline.ts` → totalImpressions | ✅已实现 |
| - 完成率 | actual/target*100 | 系统计算 | `calculateKPICompletion()` | ✅已实现 |
| **总阅读** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='read' | KpiTarget model | ✅已实现 |
| - 实际达成 | SUM(read_num) | `notes.read_num` | `pipeline.ts` → totalReads | ✅已实现 |
| - 完成率 | actual/target*100 | 系统计算 | `calculateKPICompletion()` | ✅已实现 |
| **总互动** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='engagement' | KpiTarget model | ✅已实现 |
| - 实际达成 | IF(口径=含关注, like+fav+cmt+share+follow, like+fav+cmt+share) | `notes.*_num` | `calculateEngagement()` | ✅已实现 |
| - 完成率 | actual/target*100 | 系统计算 | `calculateKPICompletion()` | ✅已实现 |
| **爆文率** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='viralCount' | KpiTarget model | ✅已实现 |
| - 爆文数量 | IF(爆文方式=千互, COUNT(like+fav+cmt>=1000), COUNT(like>=1000)) | `notes.like_num/fav_num/cmt_num` | `isViralNote()` / `calculateViralRate()` | ⚠️部分(当前仅千互) |
| - 爆文率% | 爆文数/总笔记数*100 | 系统计算 | `calculateViralRate()` | ✅已实现 |
| **总CPM** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='cpm' | KpiTarget model | ✅已实现 |
| - 实际达成 | totalCost/SUM(imp_num)*1000 | notes + juguang_data | `calculateCPM()` | ✅已实现 |
| - 完成率 | KPI/actual*100 (成本指标反转) | 系统计算 | `calculateKPICompletion(isReversed=true)` | ✅已实现 |
| **总CPC** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='cpc' | KpiTarget model | ✅已实现 |
| - 实际达成 | totalCost/SUM(read_num) | notes + juguang_data | `calculateCPC()` | ✅已实现 |
| - 完成率 | KPI/actual*100 | 系统计算 | `calculateKPICompletion(isReversed=true)` | ✅已实现 |
| **总CPE** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='cpe' | KpiTarget model | ✅已实现 |
| - 实际达成 | totalCost/totalEngagement | notes + juguang_data | `calculateCPE()` | ✅已实现 |
| - 完成率 | KPI/actual*100 | 系统计算 | `calculateKPICompletion(isReversed=true)` | ✅已实现 |
| **总CTR** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets.target_value` WHERE metric_name='ctr' | KpiTarget model | ✅已实现 |
| - 实际达成 | SUM(read_num)/SUM(imp_num)*100 | `notes.read_num/imp_num` | `calculateCTR()` | ✅已实现 |
| - 完成率 | actual/KPI*100 | 系统计算 | `calculateKPICompletion()` | ✅已实现 |
| **搜索指数** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets` WHERE metric_name='search_index' | KpiTarget model | ⚠️需人工确认字段名 |
| - 实际达成 | 月搜索指数 | `lingxi_data.data_content` WHERE data_type='search_index' | LingxiData model | ⚠️需人工确认 |
| **品牌-AIPS人群总数** | | | | |
| - KPI目标 | 直接引用 | `kpi_targets` WHERE metric_name='brand_aips' | KpiTarget model | ⚠️需人工确认字段名 |
| - 实际达成 | 灵犀数据 | `lingxi_data.data_content` WHERE data_type='aips' | LingxiData model | ✅已实现 |
| **自然流曝光量** | SUM(imp_num) - SUM(impression) | notes.imp_num - juguang_data.impression | `calculateNaturalExposure()` | ✅已实现 |
| **自然流阅读量** | SUM(read_num) - SUM(click) | notes.read_num - juguang_data.click | ⚠️需新增计算函数 | ❌未实现 |
| **自然流互动量** | totalEngagement - SUM(interaction) | notes互动 - juguang_data.interaction | ⚠️需新增计算函数 | ❌未实现 |
| **自然流CPM** | totalCost/(imp_num-impression)*1000 | 组合计算 | ⚠️需新增 | ❌未实现 |
| **自然流CPC** | totalCost/(read_num-click) | 组合计算 | ⚠️需新增 | ❌未实现 |
| **自然流CPE** | totalCost/(自然流互动) | 组合计算 | ⚠️需新增 | ❌未实现 |
| **自然流CTR** | (read_num-click)/(imp_num-impression)*100 | 组合计算 | ⚠️需新增 | ❌未实现 |
| 数据解读 | AI生成 | 叙事引擎M1模块 | `generateNarrative(M1)` | ✅已实现 |

**总费用计算公式（核心）：**
```
totalCost = aboveWaterCost + underwaterCost + juguangCost
其中：
  aboveWaterCost = SUM((kolPrice + serviceFee) * discount)  -- 水上笔记
  underwaterCost = SUM(underwaterPrice)                      -- 水下笔记
  juguangCost = SUM(fee)                                     -- 聚光消耗
```
系统实现：`src/calculation/cost.ts` → `calculateProjectTotalCost()`

**互动量口径说明：**
```
IF engagementConfig.includeFollow = true AND engagementConfig.includeShare = true:
  互动量 = like_num + fav_num + cmt_num + share_num + follow_num
ELSE IF engagementConfig.includeFollow = false:
  互动量 = like_num + fav_num + cmt_num + share_num
```
系统实现：`src/calculation/engagement.ts` → `calculateEngagement()`

**生成提示词规范：**
```
基于以下数据生成数据总览解读：
- 项目名称：{{project_name}}，品牌：{{brand}}
- 总笔记{{note_count}}篇，总费用{{total_cost}}元
- 总曝光{{total_impressions}}，KPI完成率{{impression_completion}}%
- 总阅读{{total_reads}}，KPI完成率{{read_completion}}%
- 总互动{{total_engagement}}，KPI完成率{{engagement_completion}}%
- CPM={{cpm}}，CPC={{cpc}}，CPE={{cpe}}，CTR={{ctr}}%
- 爆文{{viral_count}}篇，爆文率{{viral_rate}}%

请输出：
1. 整体数据表现概述（50字内）
2. 核心亮点指标（完成率>100%的指标）
3. 需关注指标（完成率<100%的指标）
4. 综合评价（30字内）
```


---

### 第4章：项目亮点（2-4页）

**所需数据与计算公式：**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| KPI完成率亮点 | IF(完成率>100%, 标记亮点) | kpi_targets + calculated_metrics | `identifyHighlights()` → type='kpi_exceeded' | ✅已实现 |
| 优于大盘标记 | IF(成本指标<大盘, 标记亮点) | manual_inputs(benchmark) + 实际值 | `identifyHighlights()` → type='above_benchmark' | ✅已实现 |
| 大盘对比百分比 | (benchmark-actual)/benchmark*100 (成本) | 系统计算 | `calculateBenchmarkComparison()` | ✅已实现 |
| 品牌声量SOC/SOV | 截图展示 | `lingxi_data` WHERE data_type='soc_sov' | LingxiData model | ✅已实现(数据) |
| 品牌认知-搜索排名 | 直接引用 | `lingxi_data` WHERE data_type='brand_ranking' | LingxiData model | ✅已实现 |
| 用户正向口碑 | 截图展示 | ⚠️需千瓜平台截图上传 | — | ❌未实现(需人工上传) |
| 品牌热度-搜索指数 | 直接引用 | `lingxi_data.data_content` | LingxiData model | ✅已实现 |
| 品牌心智-AIPS | 截图展示 | `lingxi_data` WHERE data_type='aips' | LingxiData model | ✅已实现(数据) |
| 内容传播-爆文 | 引用爆文数量+明细 | notes + viral计算 | `calculateViralRate()` | ✅已实现 |
| 数据解读 | AI生成 | 叙事引擎M3模块 | `generateNarrative(M3)` | ✅已实现 |

**亮点识别逻辑（系统已实现）：**
```typescript
// src/calculation/highlights.ts
// 1. KPI超额: actual/target > 1 (非成本) 或 target/actual > 1 (成本)
// 2. 优于大盘: actual < benchmark (成本) 或 actual > benchmark (非成本)
// 3. 投后优于投前: postCampaign[metric] > preCampaign[metric]
```

**生成提示词规范：**
```
基于以下亮点数据生成项目亮点解读：
- KPI超额完成指标：{{kpi_exceeded_list}}
- 优于大盘指标：{{above_benchmark_list}}
- 品牌声量数据：SOC={{soc}}%, SOV={{sov}}%
- 爆文情况：{{viral_count}}篇爆文，爆文率{{viral_rate}}%
- AIPS人群：A={{awareness}}, I={{interest}}, P={{purchase}}, S={{share}}

请按AIPS框架（Awareness→Interest→True Interest→Share）组织亮点：
1. 每个维度选取1-2个最突出的数据点
2. 使用"优于大盘XX%"的对比标记
3. 每个亮点配合关键数据卡片描述
```

---

### 第5章：综合分析/四象限（2-4页）

**所需数据与计算公式：**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 笔记质量得分 | GROUP BY note_id, 笔记CPM/CPC/CPE/CTR归一化排名 | notes + juguang_data + business_annotations | ⚠️需新增四象限计算模块 | ❌未实现 |
| 投流质量得分 | GROUP BY note_id, 投流CPM/CPC/CPE/CTR归一化排名 | juguang_data | ⚠️需新增 | ❌未实现 |
| 象限归属 | IF(笔记>=均值 AND 投流>=均值, '核心资产', ...) | 系统计算 | ⚠️需新增 | ❌未实现 |
| 四象限分布图数据 | X=投流指标, Y=笔记指标, 气泡=总费用 | 组合计算 | ⚠️需新增 | ❌未实现 |
| 各象限汇总 | GROUP BY 象限, COUNT+SUM | 系统计算 | ⚠️需新增 | ❌未实现 |
| 笔记数据总览表 | 每条笔记的完整指标 | notes + juguang_data + annotations | 部分已有 | ⚠️需整合 |

**笔记级指标计算公式：**
```
笔记CPM = (kolPrice + serviceFee + fee) / imp_num * 1000
笔记CPC = (kolPrice + serviceFee + fee) / read_num
笔记CPE = (kolPrice + serviceFee + fee) / IF(口径=含关注, like+fav+cmt+share+follow, like+fav+cmt+share)
笔记CTR = read_num / imp_num
投流CPM = fee / impression * 1000
投流CPC = fee / click
投流CPE = fee / interaction
投流CTR = click / impression
```

**象限分类规则：**
```
核心资产: 笔记质量得分 >= 项目均值 AND 投流质量得分 >= 项目均值
潜力内容: 笔记质量得分 >= 项目均值 AND 投流质量得分 < 项目均值
流量消耗: 笔记质量得分 < 项目均值 AND 投流质量得分 >= 项目均值
淘汰候选: 笔记质量得分 < 项目均值 AND 投流质量得分 < 项目均值
```

**生成提示词规范：**
```
基于四象限分析数据生成综合分析解读：
- 当前选中指标：{{selected_metric}}（CPE/CPM/CPC/CTR）
- 核心资产{{core_count}}条：特征={{core_features}}
- 潜力内容{{potential_count}}条：特征={{potential_features}}
- 流量消耗{{traffic_count}}条：特征={{traffic_features}}
- 淘汰候选{{eliminate_count}}条：特征={{eliminate_features}}

请输出：
1. 各象限数量分布概述
2. 核心资产特征+复用建议
3. 潜力内容特征+优化方向
4. 流量消耗特征+投流调整建议
5. 淘汰候选特征+替换建议
```


---

### 第6章：内容分析/笔记侧（4-8页）

**6.1 内容方向分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 内容方向(分组) | GROUP BY content_direction | `business_annotations.content_direction` | `aggregateByDimension('contentDirection')` | ✅已实现 |
| 总费用 | GROUP BY, SUM(kolPrice+serviceFee+fee) | notes + juguang_data | ⚠️当前content.ts仅用kolPrice+serviceFee | ⚠️需补充聚光fee |
| 发布数量 | GROUP BY, COUNT(note_id) | notes | `aggregateByDimension()` → noteCount | ✅已实现 |
| 曝光量 | GROUP BY, SUM(imp_num) | notes.imp_num | `aggregateByDimension()` → totalImpressions | ✅已实现 |
| 阅读量 | GROUP BY, SUM(read_num) | notes.read_num | `aggregateByDimension()` → totalReads | ✅已实现 |
| 互动量 | GROUP BY, SUM(按口径) | notes.*_num | `aggregateByDimension()` → totalEngagement | ✅已实现 |
| CTR | GROUP BY, SUM(read)/SUM(imp)*100 | notes | ⚠️当前aggregation未输出CTR | ❌未实现 |
| CPM | GROUP BY, 费用/SUM(imp)*1000 | notes + juguang | ⚠️当前aggregation未输出CPM | ❌未实现 |
| CPC | GROUP BY, 费用/SUM(read) | notes + juguang | ⚠️当前aggregation未输出CPC | ❌未实现 |
| CPE | GROUP BY, 费用/互动量 | notes + juguang | `aggregateByDimension()` → cpe | ✅已实现 |
| TI人群 | GROUP BY, SUM(ti_user_num) | juguang_data.ti_user_num (按note_id关联) | ⚠️需新增 | ❌未实现 |
| CPTI | GROUP BY, 费用/SUM(ti_user_num) | 组合计算 | ⚠️需新增 | ❌未实现 |
| 爆文率 | GROUP BY, 爆文数/总数*100 | notes | `aggregateByDimension()` → viralRate | ✅已实现 |
| 数据解读 | AI生成 | 叙事引擎M5模块 | `generateNarrative(M5)` | ✅已实现 |

**6.2 内容形式分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 内容形式(分组) | GROUP BY note_type (1=图文,2=视频) | `notes.note_type` | `aggregateByDimension('noteType')` | ✅已实现 |
| (同上各指标) | 同内容方向 | 同上 | 同上 | 同上 |

**6.3 合作形式分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 合作形式(分组) | GROUP BY 是否报备 | `business_annotations.is_underwater` (报备=非水下) | ⚠️需映射 | ⚠️需人工确认 |
| (同上各指标) | 同内容方向 | 同上 | 同上 | 同上 |

> **⚠️人工确认**：业务底表中"合作形式"字段（报备/非报备）与系统中`is_underwater`的映射关系需确认。当前系统用`isUnderwater`标记水下合作，但"报备/非报备"可能是独立维度。

**6.4 达人层级分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 达人层级(分组) | IF(粉丝<1w,'KOC', IF(<5w,'尾部', IF(<10w,'腰尾部', IF(<50w,'腰部','头部')))) | `notes.kol_fan_num` | `classifyKOLTier()` + `aggregateByKOLTier()` | ✅已实现 |
| 总费用 | GROUP BY 层级, SUM(费用) | notes | `aggregateByKOLTier()` | ✅已实现 |
| 投放数量 | GROUP BY 层级, COUNT | notes | `aggregateByKOLTier()` → noteCount | ✅已实现 |
| 曝光量 | GROUP BY 层级, SUM(imp_num) | notes | `aggregateByKOLTier()` → totalImpressions | ✅已实现 |
| 阅读量 | GROUP BY 层级, SUM(read_num) | notes | `aggregateByKOLTier()` → totalReads | ✅已实现 |
| 互动量 | GROUP BY 层级, SUM(按口径) | notes | `aggregateByKOLTier()` → totalEngagement | ✅已实现 |
| CPE | GROUP BY 层级, 费用/互动 | notes | `aggregateByKOLTier()` → averageCPE | ✅已实现 |
| 爆文率 | GROUP BY 层级, 爆文数/总数 | notes | `aggregateByKOLTier()` → viralRate | ✅已实现 |
| TI人群 | GROUP BY 层级, SUM(ti_user_num) | juguang_data | ⚠️需新增 | ❌未实现 |
| CPTI | GROUP BY 层级, 费用/TI | 组合计算 | ⚠️需新增 | ❌未实现 |
| CTR/CPM/CPC | GROUP BY 层级 | 组合计算 | ⚠️需新增 | ❌未实现 |

> **注意**：达人层级定义支持业务自定义（`review_configs.influencer_tiers`），但当前`classifyKOLTier()`使用硬编码阈值。需要改造为读取配置。

**6.5 优质笔记TOP5分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| TOP5笔记 | IF(千互, 按like+fav+cmt降序TOP5, 按like降序TOP5) | notes | ⚠️需新增排序逻辑 | ❌未实现 |
| 封面图 | 截图 | 千瓜平台 | — | ❌未实现(需人工上传) |
| 封面分析 | AI分析图片 | Vision LLM | — | ❌未实现(待开发) |
| 标题分析 | 笔记标题 | `notes` 表无note_title字段 | ⚠️需人工确认 | ❌未实现 |
| 内容分析 | AI分析视频 | Vision LLM | — | ❌未实现(待开发) |

> **⚠️人工确认**：蒲公英API返回`note_title`字段，但当前DB schema中`notes`表未存储笔记标题。需确认是否需要新增字段。

**生成提示词规范（内容分析）：**
```
基于以下内容分析数据生成解读：
- 内容方向分布：{{content_direction_breakdown}}
- 各方向核心指标：{{direction_metrics_table}}
- 内容形式对比：图文{{image_count}}篇 vs 视频{{video_count}}篇
- 达人层级分布：{{kol_tier_breakdown}}

请输出：
1. 最优内容方向及原因分析（哪个方向CPE最低/互动最高）
2. 图文vs视频效率对比结论
3. 达人层级投入产出比分析
4. 爆文集中在哪些方向/层级
5. 优化建议：下一期应加大/减少哪些方向投入
```


---

### 第7章：投流分析（3-8页）

**7.1 投流总览**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 总消耗 | SUM(fee) | `juguang_data.fee` | `pipeline.ts` → juguangAggregated.totalFee | ✅已实现 |
| 投流总曝光 | SUM(impression) | `juguang_data.impression` | `pipeline.ts` → juguangAggregated.totalImpression | ✅已实现 |
| 投流总阅读 | SUM(click) | `juguang_data.click` | `pipeline.ts` → juguangAggregated.totalClick | ✅已实现 |
| 投流总互动 | SUM(interaction) | `juguang_data.interaction` | `pipeline.ts` → juguangAggregated.totalInteraction | ✅已实现 |
| 投流CTR | SUM(click)/SUM(impression)*100 | juguang_data | `calculatePaidTrafficMetrics()` → ctr | ✅已实现 |
| 投流CPM | SUM(fee)/SUM(impression)*1000 | juguang_data | `calculatePaidTrafficMetrics()` → cpm | ✅已实现 |
| 投流CPC | SUM(fee)/SUM(click) | juguang_data | `calculatePaidTrafficMetrics()` → cpc | ✅已实现 |
| 投流CPE | SUM(fee)/SUM(interaction) | juguang_data | `calculatePaidTrafficMetrics()` → cpe | ✅已实现 |
| 新增种草人群 | SUM(i_user_num) | `juguang_data.i_user_num` | `calculatePaidTrafficMetrics()` → iUserNum | ✅已实现 |
| 种草人群成本 | SUM(fee)/SUM(i_user_num) | juguang_data | `calculatePaidTrafficMetrics()` → iUserPrice | ✅已实现 |
| 新增深度种草 | SUM(ti_user_num) | `juguang_data.ti_user_num` | `calculatePaidTrafficMetrics()` → tiUserNum | ✅已实现 |
| 深度种草成本 | SUM(fee)/SUM(ti_user_num) | juguang_data | `calculatePaidTrafficMetrics()` → tiUserPrice | ✅已实现 |
| 数据解读 | AI生成 | 叙事引擎M7模块 | `generateNarrative(M7)` | ✅已实现 |

**7.2 投放位置（广告类型）分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 广告类型(分组) | GROUP BY 广告类型 | `juguang_data` 无广告类型字段 | — | ❌未实现 |
| 消费 | GROUP BY, SUM(fee) | juguang_data.fee | — | ❌未实现 |
| 展现量 | GROUP BY, SUM(impression) | juguang_data.impression | — | ❌未实现 |
| 点击量 | GROUP BY, SUM(click) | juguang_data.click | — | ❌未实现 |
| 互动量 | GROUP BY, SUM(interaction) | juguang_data.interaction | — | ❌未实现 |
| CTR/CPM/CPC/CPE | GROUP BY计算 | 组合 | — | ❌未实现 |
| 种草人群/成本 | GROUP BY, SUM | juguang_data | — | ❌未实现 |

> **⚠️人工确认**：当前`juguang_data`表无`广告类型`字段。聚光Excel底表中有此维度（信息流/视频流/搜索），但上传解析时未提取。需要：
> 1. 在`juguang_data`表新增`ad_type`字段
> 2. 在Excel解析器中增加广告类型列识别

**7.3 人群定向分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 精准定向(分组) | GROUP BY 精准定向 | `juguang_data` 无定向字段 | — | ❌未实现 |
| (同7.2各指标) | 同上 | 同上 | — | ❌未实现 |

> **⚠️人工确认**：需在`juguang_data`表新增`targeting_type`字段，并在Excel解析器中增加识别。

**7.4 关键词定向分析**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 关键词(分组) | GROUP BY 关键词/搜索主题 | `juguang_data` 无关键词字段 | — | ❌未实现 |
| (同7.2各指标) | 同上 | 同上 | — | ❌未实现 |

> **⚠️人工确认**：需在`juguang_data`表新增`keyword`字段，并在Excel解析器中增加识别。

**7.5 笔记（创作者）维度**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 博主昵称 | GROUP BY note_id → 关联notes.kol_nick_name | juguang_data.note_id JOIN notes | 可通过JOIN实现 | ⚠️需整合 |
| 消费 | GROUP BY note_id, SUM(fee) | juguang_data | 可实现 | ⚠️需整合 |
| (同7.2各指标) | 同上 | 同上 | 可实现 | ⚠️需整合 |

**7.6 内容方向维度**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 内容方向(分组) | GROUP BY, 按note_id关联business_annotations | juguang_data JOIN business_annotations | 可通过JOIN实现 | ⚠️需整合 |
| (同7.2各指标) | 同上 | 同上 | 可实现 | ⚠️需整合 |

**7.7 搜索-词类效果（非必展示）**

| 输出值 | 计算公式 | 来源字段 | 系统实现 | 状态 |
|-------|---------|---------|---------|------|
| 词类(分组) | GROUP BY 词类标注 | `business_annotations` 无词类字段 | — | ❌未实现 |

> **⚠️人工确认**：业务底表中有"词类标注"（品牌词/品类词/场景词），当前`business_annotations`表未存储此字段。需确认是否新增。

**生成提示词规范（投流分析）：**
```
基于以下投流数据生成分析解读：
- 总消耗：{{total_fee}}元
- 投流曝光：{{total_impression}}，CTR：{{traffic_ctr}}%
- 投流CPM：{{traffic_cpm}}，CPC：{{traffic_cpc}}，CPE：{{traffic_cpe}}
- 新增种草人群：{{i_user_num}}，成本：{{i_user_price}}
- 新增深度种草：{{ti_user_num}}，CPTI：{{ti_user_price}}
- 广告类型分布：{{ad_type_breakdown}}
- 人群定向效果：{{targeting_breakdown}}

请输出：
1. 投流整体效率评价（与大盘对比）
2. 最优广告类型及原因
3. 最优人群定向及原因
4. 种草人群获取效率分析
5. 投流优化建议
```


---

### 第8章：人群资产分析（1-3页，条件展示）

**所需数据：**

| 输出值 | 计算/来源 | 系统字段 | 状态 |
|-------|----------|---------|------|
| AIPS各层级人数 | 灵犀数据 | `lingxi_data.data_content` WHERE data_type='aips' → {awareness, interest, purchase, share} | ✅已实现 |
| 流转率 | 灵犀数据 | `lingxi_data.data_content.flowRates` | ✅已实现 |
| 渗透率 | 灵犀数据 | `lingxi_data.data_content.penetrationRate` | ✅已实现 |
| TI人群总量 | SUM(ti_user_num) | `juguang_data.ti_user_num` | ✅已实现 |
| 品牌-AIPS KPI | 直接引用 | `kpi_targets` WHERE metric_name='brand_aips' | ⚠️需人工确认metric_name |
| SPU-AIPS KPI | 直接引用 | `kpi_targets` WHERE metric_name='spu_aips' | ⚠️需人工确认metric_name |
| 品牌-TI KPI | 直接引用 | `kpi_targets` WHERE metric_name='brand_ti' | ⚠️需人工确认metric_name |
| SPU-TI KPI | 直接引用 | `kpi_targets` WHERE metric_name='spu_ti' | ⚠️需人工确认metric_name |

**展示条件：** 有灵犀AIPS数据时展示（`lingxi_data` WHERE data_type='aips' EXISTS）

**生成提示词规范：**
```
基于以下人群资产数据生成分析：
- AIPS人群：A={{awareness}}万, I={{interest}}万, P={{purchase}}万, S={{share}}万
- 流转率：A→I={{a_to_i}}%, I→P={{i_to_p}}%, P→S={{p_to_s}}%
- 渗透率：{{penetration_rate}}%
- TI人群增量：{{ti_increment}}万

请输出：
1. 人群资产整体规模评价
2. 各层级流转效率分析
3. 与行业平均流转率对比（如有）
4. 人群运营建议
```

---

### 第9章：优化建议（1-3页）

**所需数据：**

| 输出值 | 计算/来源 | 系统字段 | 状态 |
|-------|----------|---------|------|
| AI优化建议初稿 | 基于全报告数据AI生成 | 叙事引擎M8模块 | `generateNarrative(M8)` | ✅已实现 |
| 人工编辑后建议 | 人工编辑 | `ai_generated_content.edited_content` WHERE content_type='optimization' | ✅已实现 |

**AI生成所需的完整数据上下文：**
```typescript
// 来自 generate route 的 baseContext
{
  project_name, brand, category, project_type,
  total_impressions, total_reads, total_engagement,
  total_cost, note_count, viral_count,
  kol_breakdown,        // 达人层级分布
  content_breakdown,    // 内容方向分布
  phase_breakdown,      // 投放阶段分布
  kpi_details,          // 各KPI完成情况
  juguang_cost,         // 投流总消耗
  juguang_impressions,  // 投流总曝光
  project_objective,    // 项目目标(策划案)
  project_strategy,     // 项目策略(策划案)
  target_audience,      // 目标人群(策划案)
}
```

**生成提示词规范：**
```
基于以下复盘数据生成优化建议：
- 项目类型：{{project_type}}，品牌：{{brand}}
- KPI完成情况：{{kpi_details}}
- 内容方向表现：{{content_breakdown}}
- 达人层级表现：{{kol_breakdown}}
- 投流效率：CPM={{traffic_cpm}}, CPC={{traffic_cpc}}, CPTI={{ti_user_price}}
- 爆文率：{{viral_rate}}%，集中方向：{{viral_directions}}

请按以下维度输出优化建议（每维度2-3条bullet points）：
1. 内容策略优化：哪些内容方向应加大/减少，内容形式建议
2. 达人选择优化：层级配比调整，高ROI达人特征
3. 投流策略优化：广告类型配比，人群定向调整，关键词优化
4. 执行过程优化：节奏安排，协作效率
5. 后续项目建议：可复用经验，需规避问题
```

---

### 第10章：尾页（1页）

**所需数据：**

| 输出值 | 来源 | 系统字段 | 状态 |
|-------|------|---------|------|
| THANKS | 固定文案 | — | ✅ |
| 项目周期 | 直接引用 | `projects.start_date` ~ `projects.end_date` | ✅已实现 |


---

## 三、计算引擎已实现函数清单

| 函数名 | 文件 | 功能 | 输入 | 输出 |
|--------|------|------|------|------|
| `calculateProjectTotalCost` | cost.ts | 项目总费用 | CostCalculationInput | ProjectCost |
| `calculateEngagement` | engagement.ts | 互动量(可配口径) | NoteMetrics + EngagementConfig | number |
| `calculateCPE` | metrics.ts | 每互动成本 | totalCost, totalEngagement | number or N/A |
| `calculateCPM` | metrics.ts | 千次曝光成本 | totalCost, totalImpressions | number or N/A |
| `calculateCPC` | metrics.ts | 每阅读成本 | totalCost, totalReads | number or N/A |
| `calculateCTR` | metrics.ts | 点击率 | totalReads, totalImpressions | number or N/A |
| `calculatePaidTrafficMetrics` | metrics.ts | 投流全指标 | JuguangAggregated | PaidTrafficMetrics |
| `isViralNote` | viral.ts | 爆文判定 | NoteMetrics | boolean |
| `calculateViralRate` | viral.ts | 爆文率 | NoteMetrics[] | {viralCount, viralRate} |
| `calculateKPICompletion` | kpi.ts | KPI完成率 | actual, target, isReversed | KPIResult |
| `classifyKOLTier` | kol-tier.ts | 达人层级分类 | fanCount | KOLTier |
| `aggregateByKOLTier` | kol-tier.ts | 按层级聚合 | NoteWithKOL[] | KOLTierAggregation[] |
| `calculateNaturalExposure` | traffic.ts | 自然曝光 | pugongying, juguang impressions | NaturalExposureResult |
| `aggregateByDimension` | content.ts | 按维度聚合 | AnnotatedNote[], dimension | DimensionAggregation[] |
| `calculateBenchmarkComparison` | benchmark.ts | 大盘对比 | actual, benchmark, isCost | BenchmarkResult |
| `identifyHighlights` | highlights.ts | 亮点识别 | ProjectMetrics, Benchmarks, KPIs | Highlight[] |
| `calculateComponentConversion` | components.ts | 组件转化 | ComponentData[] | ComponentMetrics[] |
| `runCalculationPipeline` | pipeline.ts | 全量计算 | projectId | void (写入DB) |

---

## 四、第三方API接口对接情况

### 4.1 派查查API（Paichacha Client）

**文件：** `src/ingestion/paichacha-client.ts`

| 接口 | 端点 | 功能 | 返回数据 | 状态 |
|------|------|------|---------|------|
| 蒲公英笔记数据 | POST `/api/pugongying/notes` | 批量获取笔记指标 | PugongyingNote[] | ✅已实现 |
| 聚光笔记数据 | POST `/api/juguang/notes` | 批量获取投流指标 | JuguangNote[] | ✅已实现 |

**请求格式：** `{ note_ids: string[] }`
**认证方式：** Bearer Token (`PAICHACHA_API_KEY`)
**重试策略：** 指数退避，最多3次重试
**金额转换：** API返回分，系统自动转元（`normalizeAmount`）

### 4.2 Excel上传解析

**文件：** `web/src/app/api/upload/combined/route.ts`

| 支持的Sheet类型 | 检测方式 | 解析字段 | 状态 |
|---------------|---------|---------|------|
| 蒲公英数据 | 有"博主昵称"+"曝光量"列 | 全部笔记指标 | ✅已实现 |
| 聚光数据 | 有"消耗/费用"列 | fee/impression/click/interaction/i_user/ti_user | ✅已实现 |
| 业务标注 | 有"笔记id"列 | content_direction/account_type/kol_type/launch_phase | ✅已实现 |

**聚光Excel列名别名（已支持）：**
- fee: `fee`, `消耗`, `花费`, `费用`, `消费`
- impression: `impression`, `曝光`, `曝光量`, `展现量`
- click: `click`, `点击`, `点击量`
- interaction: `interaction`, `互动`, `互动量`
- i_user_num: `i_user_num`, `i人群`, `兴趣人群`, `新增种草人群`
- ti_user_num: `ti_user_num`, `ti人群`, `深度兴趣人群`, `新增深度种草人群`

### 4.3 策划案解析（Vision LLM）

**文件：** `web/src/app/api/upload/plan-parse/`

| 功能 | 实现方式 | 输出 | 状态 |
|------|---------|------|------|
| PDF策划案解析 | PDF→图片→Vision LLM批量识别 | projectObjective, strategy, targetAudience, coreMessage | ✅已实现 |

### 4.4 叙事生成（LLM）

**文件：** `src/engines/narrative.ts` + `src/report/llm-client.ts`

| 功能 | 模板路径 | 模块 | 状态 |
|------|---------|------|------|
| 数据总览解读 | prompts/{type}/M1_overview/{tone}.yaml | M1 | ✅已实现 |
| 项目回顾 | prompts/{type}/M2_review/{tone}.yaml | M2 | ✅已实现 |
| 项目亮点 | prompts/{type}/M3_highlights/{tone}.yaml | M3 | ✅已实现 |
| 未达预期 | prompts/{type}/M4_underperform/{tone}.yaml | M4 | ✅已实现 |
| 内容分析 | prompts/{type}/M5_content/{tone}.yaml | M5 | ✅已实现 |
| 竞品洞察 | prompts/{type}/M6_competitor/{tone}.yaml | M6 | ✅已实现 |
| 投流分析 | prompts/{type}/M7_traffic/{tone}.yaml | M7 | ✅已实现 |
| 问题诊断 | prompts/{type}/M8_diagnosis/{tone}.yaml | M8 | ✅已实现 |


---

## 五、未实现字段与缺口汇总

### 5.1 需要新增的数据库字段

| 表 | 新增字段 | 类型 | 用途 | 优先级 |
|----|---------|------|------|--------|
| `juguang_data` | `ad_type` | VarChar(50) | 广告类型(信息流/视频流/搜索) | 高 |
| `juguang_data` | `targeting_type` | VarChar(100) | 精准定向类型 | 高 |
| `juguang_data` | `keyword` | VarChar(200) | 关键词/搜索主题 | 高 |
| `notes` | `note_title` | VarChar(500) | 笔记标题 | 中 |
| `business_annotations` | `word_category` | VarChar(50) | 词类标注(品牌词/品类词/场景词) | 中 |
| `business_annotations` | `cooperation_form` | VarChar(50) | 合作形式(报备/非报备) | 中 |

### 5.2 需要新增的计算函数

| 函数 | 用途 | 输入 | 优先级 |
|------|------|------|--------|
| `calculateNaturalReads` | 自然流阅读量 | totalReads - juguangClicks | 高 |
| `calculateNaturalEngagement` | 自然流互动量 | totalEngagement - juguangInteraction | 高 |
| `calculateNaturalCPM/CPC/CPE/CTR` | 自然流成本指标 | 组合计算 | 高 |
| `calculateNoteQualityScore` | 笔记质量得分(四象限) | 笔记级CPM/CPC/CPE/CTR归一化 | 高 |
| `calculateTrafficQualityScore` | 投流质量得分(四象限) | 投流级CPM/CPC/CPE/CTR归一化 | 高 |
| `classifyQuadrant` | 象限归属 | 笔记得分+投流得分 vs 均值 | 高 |
| `aggregateByAdType` | 按广告类型聚合 | juguang_data GROUP BY ad_type | 高 |
| `aggregateByTargeting` | 按定向聚合 | juguang_data GROUP BY targeting | 高 |
| `aggregateByKeyword` | 按关键词聚合 | juguang_data GROUP BY keyword | 中 |
| `getTopNotes` | 优质笔记TOP5 | notes排序 | 中 |
| `aggregateWithTI` | 维度聚合含TI人群 | notes+juguang JOIN | 中 |

### 5.3 需要新增的Excel解析列

| Sheet类型 | 新增列识别 | 别名 | 优先级 |
|----------|-----------|------|--------|
| 聚光 | 广告类型 | `广告类型`, `投放位置`, `ad_type` | 高 |
| 聚光 | 精准定向 | `精准定向`, `人群定向`, `定向类型`, `targeting` | 高 |
| 聚光 | 关键词 | `关键词`, `搜索主题`, `搜索词`, `keyword` | 高 |
| 蒲公英 | 笔记标题 | `笔记标题`, `标题`, `note_title` | 中 |
| 业务底表 | 词类标注 | `词类`, `词类标注`, `word_category` | 中 |
| 业务底表 | 合作形式 | `合作形式`, `是否报备`, `cooperation_form` | 中 |

### 5.4 需人工确认的映射关系

| 序号 | 问题 | 影响范围 | 建议 |
|------|------|---------|------|
| 1 | `kpi_targets.metric_name` 中搜索指数的字段名是什么？ | 数据总览-搜索指数 | 建议用 `search_index` |
| 2 | `kpi_targets.metric_name` 中AIPS相关KPI的字段名？ | 数据总览-AIPS | 建议用 `brand_aips`/`spu_aips`/`brand_ti`/`spu_ti` |
| 3 | 业务底表"合作形式"与系统`is_underwater`的关系？ | 内容分析-合作形式 | 报备≠水上，需独立字段 |
| 4 | 达人层级是否支持业务自定义范围？ | 内容分析-达人层级 | 当前硬编码，需改为读取review_configs.influencer_tiers |
| 5 | 爆文计算方式"千赞"(like>=1000)是否需要支持？ | 全报告爆文相关 | 当前仅支持千互(like+fav+cmt>=1000) |
| 6 | 聚光数据是否需要按时间段GROUP BY？ | 投流总览-分时段 | 当前juguang_data无日期字段 |
| 7 | 灵犀"月搜索指数"的具体数据结构？ | 数据总览-搜索指数 | 需确认lingxi_data.data_content格式 |
| 8 | 蒲公英API是否返回`note_title`？ | 优质笔记分析 | paichacha-client未映射此字段 |

---

## 六、数据流转全链路

```
┌─────────────────────────────────────────────────────────────────┐
│                        数据输入层                                 │
├─────────────────────────────────────────────────────────────────┤
│ 1. Excel上传 (/api/upload/combined)                              │
│    → 蒲公英Sheet → notes表                                       │
│    → 聚光Sheet → juguang_data表                                  │
│    → 业务标注Sheet → business_annotations表                       │
│                                                                   │
│ 2. 派查查API (PaichachaClient)                                   │
│    → fetchPugongyingData → notes表                               │
│    → fetchJuguangData → juguang_data表                           │
│                                                                   │
│ 3. 灵犀数据上传 (/api/upload/external)                           │
│    → SpreadsheetParser → lingxi_data表                           │
│                                                                   │
│ 4. 人工录入 (/api/upload/manual)                                 │
│    → 大盘基准 → manual_inputs表                                   │
│    → KPI目标 → kpi_targets表                                     │
│                                                                   │
│ 5. 策划案上传 (/api/upload/plan-parse)                           │
│    → Vision LLM解析 → ai_generated_content表                     │
│                                                                   │
│ 6. 复盘配置 (/api/reviews POST)                                  │
│    → review_configs表                                             │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        计算引擎层                                 │
├─────────────────────────────────────────────────────────────────┤
│ runCalculationPipeline(projectId)                                 │
│   → calculateProjectTotalCost → project_total_cost               │
│   → calculateEngagement (per note) → engagement                  │
│   → calculateCPE/CPM/CPC/CTR → core_metrics                     │
│   → calculateViralRate → viral                                   │
│   → calculateKPICompletion → kpi_completion                      │
│   → aggregateByKOLTier → kol_tier                                │
│   → calculateNaturalExposure → natural_exposure                  │
│   → calculatePaidTrafficMetrics → paid_traffic                   │
│   → aggregateByDimension (5维度) → dimension_aggregations        │
│   → calculateBenchmarkComparison → benchmark_comparison          │
│   → identifyHighlights → highlights                              │
│   → calculateComponentConversion → component_conversion          │
│   结果写入 → calculated_metrics表                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        智能引擎层                                 │
├─────────────────────────────────────────────────────────────────┤
│ POST /api/generate/[projectId]                                    │
│   → Rating Engine (rateAllMetrics) → MetricRating[]              │
│   → Decision Engine (decideModules) → ModuleDecision[]           │
│   → Narrative Engine (generateNarrative per module)              │
│     → loadTemplate(YAML) → substituteVariables → LLM call       │
│   结果写入 → report_versions表 + module_decisions表              │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        输出层                                     │
├─────────────────────────────────────────────────────────────────┤
│ 逐章节报告生成 (ReportPipelineOrchestrator)                       │
│   → 读取 chapter prompt templates (src/prompts/chapters/)        │
│   → 逐章加载数据 (ChapterDataLoaderRegistry)                     │
│   → 填充模板变量 → 调用LLM → 解析响应                            │
│   → 结果写入 report_versions表 + review_configs.reportContent    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 七、与业务字段映射表的差异对照

| 业务要求 | 系统现状 | 差异说明 | 处理建议 |
|---------|---------|---------|---------|
| 内容金额口径二选一(消耗/结算) | 系统统一用kolPrice+serviceFee | 无"结算金额"独立字段 | ⚠️需确认：当前kolPrice是否=实际结算金额 |
| 互动量口径含/不含关注 | ✅已实现 engagementConfig | 一致 | — |
| 爆文方式千互/千赞 | 仅实现千互(like+fav+cmt>=1000) | 缺千赞(like>=1000) | 需扩展viralMetric配置 |
| 达人层级自定义 | 硬编码5级 | 业务要求自定义名称+范围 | 需改造classifyKOLTier读取配置 |
| 聚光按广告类型/定向/关键词分组 | 无分组维度字段 | juguang_data缺少维度字段 | 需新增字段+解析 |
| 自然流各指标 | 仅实现自然曝光 | 缺自然阅读/互动/CPM/CPC/CPE/CTR | 需新增计算函数 |
| 四象限分析 | 未实现 | 需笔记级+投流级得分计算 | 需新增完整模块 |
| 搜索指数(灵犀) | lingxi_data可存储 | 具体字段名未确认 | 需人工确认数据格式 |
| 品牌/SPU AIPS KPI | kpi_targets可存储 | metric_name命名未确认 | 需人工确认 |
| 笔记标题 | notes表无此字段 | 蒲公英API有返回 | 需新增字段 |
| 词类标注 | business_annotations无此字段 | 业务底表有此维度 | 需新增字段 |
| 合作形式(报备/非报备) | 仅有is_underwater | 报备≠非水下 | 需新增独立字段 |
| 投流分时段分析 | juguang_data无日期字段 | 业务要求按时间段GROUP BY | 需新增date字段 |

---

*文档生成时间：2026-05-25*
*数据来源：prisma/schema.prisma + src/calculation/* + src/ingestion/* + web/src/app/api/* + git log (May 21后提交)*
